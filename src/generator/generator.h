#pragma once
#include "generator/params.h"
#include <vector>

std::vector<double> generate_signal(const GeneratorParams params);
std::vector<double> generate_signal_keys(const GeneratorParams params);